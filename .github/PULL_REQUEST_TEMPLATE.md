## What this changes

<!-- What the change does, and why. Link the issue it closes. -->

## Notes for the reviewer

<!-- Anything non-obvious: a trade-off you made, an alternative you rejected,
     a breaking change (Stet is pre-launch, so breaking changes are fine as
     long as they are stated). Delete this section if there is nothing. -->

## Checklist

- [ ] `vp run ready` passes (`vp check`, knip, type check, build).
- [ ] Tests cover the change, or it does not need any.
- [ ] Docs updated: the package README for internals, `apps/docs` for product behaviour.
- [ ] A changeset (`pnpm changeset`) if this changes a `published/*` package.

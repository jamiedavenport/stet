{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // Enqueueing a job pulls its handlers, one of which renders React Email.
    "jsx": "react-jsx",
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "types": ["@cloudflare/workers-types"]
  },
  "include": ["src"]
}

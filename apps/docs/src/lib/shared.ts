import { brand } from '@repo/brand';

export const appName = brand.name;
export const docsRoute = '/';

// The repository is private, so public links point at the GitHub profile.
export const githubUrl = 'https://github.com/jamiedavenport';

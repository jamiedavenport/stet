/**
 * The row that ends a content table and adds to it: "New entry" under a
 * collection's entries, "New key" under a map's keys. One class list so the
 * two tables cannot drift apart.
 */
export const addRowClasses =
  'flex h-11 w-full items-center gap-2 border-b px-6 text-left text-sm text-muted-foreground hover:bg-muted/50 hover:text-foreground';
